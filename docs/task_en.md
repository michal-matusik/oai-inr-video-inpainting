# English task summary

Build an implicit neural representation that maps continuous pixel coordinates x, y, and time t to RGB values and a binary segmentation mask for a masked video region.
The input frames are 256 by 256 pixels.
The goal is to infer only the missing region from observed training examples.
